# 2026-07-03

## Session 1 — Open-issue/PR audit + implementation backlog doc

### Flow

```text
gh issue/pr list ──> 3 parallel verification agents ──> synthesis
  │                    ├─ app-feature issues vs code        │
  │                    ├─ CI/release/docs issues vs repo    ▼
  └─ PR #73 checks     └─ PR #73 diff review        .agents/docs/ISSUE_BACKLOG.md
```

### Affected components

- Docs only: `.agents/docs/ISSUE_BACKLOG.md` (new), this session file. No app code touched.

### What was done

- [x] Listed all 17 open issues + 1 open PR (#73); pulled full bodies.
- [x] Discovered the stale-state root cause: #74 integrated PRs #63–#66/#69/#70 but those PRs
      were **closed, not merged**, so their `Closes #N` never fired.
- [x] Verified every open issue against master @ `3991b1d` with three parallel agents
      (app features / CI-release-docs / PR #73 diff review).
- [x] Wrote `.agents/docs/ISSUE_BACKLOG.md` — tiered hand-off spec for the implementation pass.

### Key findings

- **Already done, close:** #17 (CI hard gate + coverage + lint), #21 (versioning — superseded
  by v1.6), #24 (docs truth-sync), #28 (print → `AppLog`; zero `print(` in app/widget).
- **Partially done, bodies stale:** #13 (reloadTimelines done; widget still has duplicate
  private `SharedDataStore` class at `MeterBarWidget/UsageWidget.swift:19-41`), #20 (packaging
  automated; stale `MeterBar/Info.plist` hardcoding 1.0 still checked in), #23 (band-crossing
  dedup exists; no user prefs), #26 (`meterbar cost` covers both providers + `--json`; `--days`
  missing), #29 (contract tests exist; no rendering validation), #40 (force-unwrap fixed;
  dashboard badges missing, `fetchUsageMetrics` still ~112 lines), #71 (checklist references
  "in review" PRs that already landed; claims R8 done but `UsageDashboardView.swift` is still
  1539 lines).
- **Not started:** #18 (no XCUITest target, no app scheme checked in), #22, #25, #27, #72.
- **Unactionable here:** #31 website refactor — site source is not in this repo (no `website/`,
  no gh-pages, no separate repo under the account).
- **PR #73:** `mergeable: CONFLICTING` (branched pre-#74). Green CI is misleading. Verdict:
  rebase + rework — keep `SocialShareCardContent` model + tests (clean, no privacy leaks),
  replace its local `tightestDashboardLimit`/`DashboardProviderSnapshot` re-derivation with
  master's `providerSnapshots.tightestLimit` (`ProviderSnapshot.swift:188`), and extract the
  ~500 lines of card views into their own file instead of appending to the dashboard monolith.

### Key decisions

- **One backlog doc, tiered by dependency** (hygiene → small fixes → shared-core features →
  test hardening → product features) rather than per-issue notes — the next session works
  top-to-bottom.
- **#22 + #25 consolidated** around a single `ProviderReadiness` core in `MeterBarShared`
  (CLI `doctor` first, then app Diagnostics view, then onboarding checklist).
- **#29's remainder folded into #18** (one e2e/smoke suite owns widget rendering validation);
  the repo's first `schedule:` workflow comes with it (daily e2e + coverage).
- **Dashboard split (R8 finish) sequenced before PR #73 rework and #72** — both add dashboard
  sections and must not grow the 1539-line file.
- GitHub-side actions (closing #17/#21/#24/#28, rewriting stale bodies) left for Vincent's
  confirmation — outward-facing changes not executed unilaterally.

### Files changed

- `.agents/docs/ISSUE_BACKLOG.md` (new)
- `.agents/SESSIONS/2026-07-03.md` (this file)

### Mistakes and fixes

- Initial hypothesis that #74 split `UsageDashboardView.swift` into per-type files was wrong —
  the diff review confirmed the file is still monolithic; the split was only partial elsewhere.
  Backlog corrected to list finishing R8 as open work.

### Next steps

- Vincent: confirm issue closures/body rewrites; decide where #31 (website) actually lives.
- Implementation pass (Opus 4.8): work `.agents/docs/ISSUE_BACKLOG.md` top-to-bottom,
  starting with Tier 0 hygiene + Tier 1 batch PR.

## Session 2 — Tier 0 execution + parallel session dispatch

Vincent approved. Executed the Tier 0 tracker cleanup:

- **Closed as done:** #17, #21 (superseded by v1.6), #24, #28 — each with an
  evidence comment.
- **Closed as consolidated:** #29 → remaining scope (widget rendering validation)
  absorbed into #18's rewritten body.
- **Rewrote bodies to verified remaining scope:** #13, #18, #20, #23, #26, #40, #71.
- **Commented:** PR #73 (rebase+rework verdict, keep model+tests, adopt
  `providerSnapshots.tightestLimit`, extract card views); #31 (unactionable —
  website source location unknown, question posted for Vincent).
- **Spawned six parallel implementation session chips** (Opus 4.8, fresh worktrees):
  Tier 1 batch, PR #73 rework, Tier 2a readiness core, Tier 2b+2c
  (notifications prefs + launch-at-login), Tier 3 test hardening, Tier 4b
  insights page (#72). Ordering constraint communicated in prompts: Tier 1
  (dashboard split) before the #73 rework and #72.
- **Not done:** merging PR #75 (permission classifier blocked self-merge — left
  for Vincent). Spawned-session prompts fall back to
  `git show origin/claude/jolly-lamport-2cde15:.agents/docs/ISSUE_BACKLOG.md`
  until it merges.

Open tracker after cleanup: 10 issues (#13 #18 #20 #22 #23 #25 #26 #27 #31 #40
#47 #71 #72 minus the five closed), every body now matching verified code state.

**Summary:** Tier 1 backlog batch (#13 #40 #26 #20)

---

## Session 3 — Tier 1 small-scoped fixes (1a–1f)

**Duration:** ~1 session
**Status:** Complete

### What was done

Implemented all six Tier-1 items from `.agents/docs/ISSUE_BACKLOG.md` as one PR, TDD where logic was involved.

- **1a — Widget `SharedDataStore` unification (rest of #13).** Added `SharedMetricsStore`
  to `MeterBarShared` owning the app-group identifier, metrics file name, container/file
  URLs, and the tolerant read path. Deleted the widget's private duplicate `SharedDataStore`
  class; the widget now calls `SharedMetricsStore.loadMetrics()`. App-side `SharedDataStore`
  keeps the write path + `reloadWidgetTimelines()` and delegates its read + location to the
  shared type. `StorageKeys.cachedUsageMetrics` re-exports `SharedMetricsStore.metricsKey`
  so the app/widget/CLI can't drift. Extended `CachedMetricsContractTests` to pin the shared
  constants and the reader's decode path.
- **1b — Dashboard badge parity (rest of #40).** New shared `ProviderStatusBadges` component
  (`MeterBar/Views/Components/`) renders the "N resets available" + "Extra usage" pill,
  `.compact` for the popover and `.regular` for the dashboard. Wired into
  `PopoverProviderStatusCard`, `ProviderOverviewStatusCard`, and `ProviderLimitsCard` — no
  copy-paste. Removed the duplicated `resetCreditsLabel` static from `MenuBarView`.
- **1c — Split `CodexCliLocalService.fetchUsageMetrics()` (rest of #40).** Extracted the
  response→`UsageMetrics` mapping into a pure `CodexCliUsageResponse.toUsageMetrics()`;
  `fetchUsageMetrics()` now just decodes + calls it. Fixture-driven unit tests
  (`CodexUsageMappingTests`) cover paid/free accounts and a missing secondary window.
- **1d — `meterbar cost --days N` (rest of #26).** Added a pure `CostSummary.dailyCostWindow`
  filter over the cached daily rows (no rescan) + `DailyCostWindow`/`ProviderDailyTotal`
  types; wired `@Option days` into the CLI `Cost` subcommand with a truncation notice when
  the cache covers fewer days than requested. Unit-tested the filter (`CostWindowTests`).
- **1e — Deleted stale `MeterBar/Info.plist` (rest of #20).** Build uses
  `GENERATE_INFOPLIST_FILE=YES`; the checked-in file hardcoded 1.0. Verified the only real
  reference was the SPM `exclude` (removed it); the widget's Info.plist and the CLI's runtime
  bundle-plist path are untouched.
- **1f — Finished R8: split `UsageDashboardView.swift`** (1539→450 lines) into
  `DashboardCard.swift`, `DashboardProviderCards.swift`, `DashboardCostCards.swift`, and
  `DailyUsageChart.swift`. Pure moves; the only edits were dropping file-scope `private` on
  moved top-level decls so they stay module-internal.

### Files changed

- `Packages/MeterBarShared/Sources/MeterBarShared/SharedMetricsStore.swift` - new shared store/constants
- `MeterBar/Models/StorageKeys.swift` - re-export shared metrics key
- `MeterBar/Services/SharedDataStore.swift` - delegate read + location to shared store
- `MeterBarWidget/UsageWidget.swift` - drop duplicate store, use shared reader
- `MeterBar/Services/CodexCliLocalService.swift` - extract `toUsageMetrics()`
- `MeterBar/Models/TokenCost.swift` - `dailyCostWindow` + `DailyCostWindow`/`ProviderDailyTotal`
- `MeterBarCLI/Sources/MeterBarCLI.swift` - `cost --days N`
- `MeterBar/Views/Components/ProviderStatusBadges.swift` - new shared badge row
- `MeterBar/Views/MenuBarView.swift` - use shared badges
- `MeterBar/Views/UsageDashboardView.swift` - trimmed to window controller + main view; badges added
- `MeterBar/Views/{DashboardCard,DashboardProviderCards,DashboardCostCards,DailyUsageChart}.swift` - R8 split
- `MeterBar/Info.plist` - deleted (stale)
- `Package.swift` - drop the stale `Info.plist` exclude
- `MeterBarTests/{CachedMetricsContractTests,CodexUsageMappingTests,CostWindowTests}.swift` - tests

### Decisions

- **Decision:** Shared read store lives in `MeterBarShared` as `SharedMetricsStore`, not a
  renamed `StorageKeys`.
  - **Context:** The app already has an app-only `StorageKeys` (Claude account keys, etc.).
  - **Rationale:** Avoids a name collision when the app imports `MeterBarShared`; keeps the
    app's UserDefaults keys out of the cross-target shared surface.
- **Decision:** `cost --days` reports only what daily rows carry (input/output/cache-read +
  cost), not cache-creation tokens or session counts.
  - **Context:** `DailyTokenUsage` doesn't store those; the dated data is the daily rows.
  - **Rationale:** Honest, rescan-free windowing; a truncation notice fires when the cache
    spans fewer days than requested rather than silently under-reporting.

### Mistakes and fixes

- **Mistake:** First pass at the R8 split sliced type ranges by declaration line, so
  `DashboardDateFormat`'s leading doc comment landed at the end of the previous file.
- **Fix:** Re-sliced with comment-aware start boundaries; verified the moved line multiset is
  byte-identical to the original (modulo added imports and dropped `private`).
- **Prevention:** When splitting Swift files, treat a type's block as starting at its leading
  comment/attribute lines, not the `struct`/`enum` keyword line.

### Verification

- Local: only Command Line Tools installed (no XCTest/SourceKit), so per policy full
  `swift test` + SwiftLint run in CI. Locally: built `MeterBarShared` (`swift build`, passes);
  verified split content preservation, brace/paren balance, and no consecutive blank lines.

### Next steps

- [ ] CI: confirm `swift test` + SwiftLint `--strict` green on the PR.
- [ ] #20 stays open — optional local `scripts/package.sh` mirroring `release.yml` not done.

---

**Summary:** Tier 3 test hardening — service-layer tests + coverage ratchet (PR 1, #78), UI/e2e smoke suite + first scheduled workflow (PR 2, #18)

---

## Session 4 — Service-layer tests + coverage ratchet (issue #71, ISSUE_BACKLOG Tier 3a)

**Duration:** ~2 hours
**Status:** Complete (PR from `test/service-layer-coverage`)

### What was done

Added protocol/closure seams to the previously-untested services and fixture-driven
unit tests, following the existing `CostTrackerTests` / contract-test patterns. No
behavior change to production paths — every seam defaults to the prior singleton wiring.

**Seams added (production):**

- `KeychainManager` — `KeychainBackend` protocol over the four `SecItem*` primitives;
  real `SecItemKeychainBackend` default, in-memory fake for tests. The login keychain is
  unavailable / prompts on CI.
- `SharedDataStore` — injectable `directoryOverride` + post-write `didWrite` hook +
  `flushPendingWrites()`. Lets the encode→atomic-write→decode round-trip run against a
  temp dir without the App Group entitlement.
- `CodexCliLocalService` — extracted pure `mapUsageResponse(_:)`; injectable
  `authFileDataProvider` closure for `~/.codex/auth.json`.
- `CursorLocalService` — extracted pure `mapSummary(_:)`; made `extractUserIdFromJWT`
  static/internal. Plan-total default + on-demand headroom now static constants.
- `UsageDataManager` — `SimpleUsageProviding` protocol (Codex/Cursor conform); injectable
  providers, `ProviderVisibilityStore`, `SharedDataStore`, cache `UserDefaults`, and a
  `schedulesAutoRefresh` flag (off in tests to avoid the `Timer`).
- `ProviderVisibilityStore.init` made internal (was private) for isolated-suite tests.
- `CostTracker.scanCodexArchivedSessions` + `CodexScanContext` made internal for
  fixture testing (Claude `parseSessionFile` was already covered).

**Tests added:** `KeychainManagerTests`, `SharedDataStoreTests`, `CodexCliLocalServiceTests`,
`CursorLocalServiceTests`, `UsageDataManagerTests`, plus Codex-scan cases in
`CostTrackerTests`. Shared `MetricsFixtures` helper (Claude/Codex/Cursor) reused by the
store + orchestration tests (and by PR 2's widget checks).

### Files changed

- `MeterBar/Services/KeychainManager.swift` — `KeychainBackend` seam
- `MeterBar/Services/SharedDataStore.swift` — dir override + write hook + flush
- `MeterBar/Services/CodexCliLocalService.swift` — pure mapping + auth seam
- `MeterBar/Services/CursorLocalService.swift` — pure mapping + static JWT extract
- `MeterBar/Services/UsageDataManager.swift` — provider seam + injectable init
- `MeterBar/Services/ProviderVisibilityStore.swift` — internal init
- `MeterBar/Services/CostTracker.swift` — internal Codex scan seam
- `MeterBarTests/{KeychainManager,SharedDataStore,CodexCliLocalService,CursorLocalService,UsageDataManager}Tests.swift` — new
- `MeterBarTests/MetricsFixtures.swift` — shared fixtures
- `MeterBarTests/CostTrackerTests.swift` — Codex archived-session cases
- `.github/workflows/ci.yml` — `COVERAGE_THRESHOLD` ratcheted after CI measured the new baseline

### Decisions

- **Seams via default-argument injection, not a DI framework.** Matches the repo's
  singleton style; `shared` behavior is byte-identical, tests pass fakes.
- **Local verification = compile only.** Only Command Line Tools are installed (no XCTest
  module), so tests are run by CI; locally verified with `swift build`.

### Next steps

- [x] PR 2: UI/e2e smoke suite + widget rendering validation + first `schedule:` workflow (#18)

---

## Session 5 — UI/e2e smoke suite + first scheduled workflow (issue #18, ISSUE_BACKLOG Tier 3b)

**Duration:** ~1 hour
**Status:** Complete (PR from `test/ui-e2e-smoke`, stacked on PR 1)

### What was done

Added a deterministic, credential-free app-level smoke suite (SwiftPM, not XCUITest —
full UI automation needs a signed `.app` + UITest target run via `xcodebuild`, which
neither the SwiftPM CI nor the daily credential-less run can drive) plus the repo's
first scheduled workflow.

- **Shared `MeterBar` app scheme** checked in (only the widget scheme existed before),
  modeled on `MeterBarWidgetExtension.xcscheme` with the app target's blueprint id.
- **`MenuBarSmokeTests`** — first flow from the issue: launch (real `UsageDataManager`
  wired to fixture providers + isolated stores via the PR 1 seams) → inject fixture
  usage data → change refresh interval → assert metrics snapshot, App Group widget-bridge
  file, persisted cache blob, and refresh-interval persistence. Plus relaunch-restores-cache.
- **`WidgetRenderingTests`** — widget rendering validation (absorbs #29 remainder):
  small/medium/large layouts render non-empty rows for Claude/Codex/Cursor fixtures
  (view-model-level — validates the `MeterBarShared` data→presentation contract the
  widget renders from, since the extension target isn't in the SwiftPM package), stable
  sort order, and empty state.
- **`.agents/docs/TESTING.md`** — documented single smoke command + manual widget QA
  checklist (populated / single / empty / stale / error / reload).
- **`.github/workflows/e2e.yml`** — first `schedule:` workflow (daily 09:17 UTC +
  `workflow_dispatch`): full `swift test` + coverage gate + app/widget `xcodebuild`,
  fixtures only. Per-PR CI already runs the smoke tests via the `test` job.

### Files changed

- `MeterBar.xcodeproj/xcshareddata/xcschemes/MeterBar.xcscheme` — new shared app scheme
- `MeterBarTests/MenuBarSmokeTests.swift`, `MeterBarTests/WidgetRenderingTests.swift` — new
- `.agents/docs/TESTING.md` — smoke command + manual widget checklist
- `.github/workflows/e2e.yml` — new daily scheduled workflow

### Decisions

- **View-model-level widget validation, widget target untouched.** No full Xcode locally
  and the per-PR CI skips the widget `xcodebuild`, so modifying the extension risked a
  broken widget landing on a green PR. Validated the shared render contract instead; the
  daily workflow builds the widget as a standing safety net.
- **Smoke suite as SwiftPM tests.** Runs in the existing PR `test` job (deterministic
  subset) and the new daily workflow (full), satisfying #18's CI requirement without a
  macOS UITest target the environment can't execute.

### Next steps

- [ ] Optional follow-up: have `MeterBarWidget/UsageWidget.swift` consume a shared render
      model so the widget and its contract test share one source of truth (kept out of
      this PR to avoid touching the unverifiable-locally widget target).

---

**Total sessions today:** 2

---

**Summary:** Notification prefs + Launch at Login

---

## Session 6 — Tier 2b + 2c — notification preferences & Launch at Login

**Duration:** ~1 hour
**Status:** Complete (pending CI)

### What was done

Implemented backlog Tiers 2b + 2c (issues #23 and #27) in one PR, TDD-style.

**2b — Notification preferences (#23):**
- Kept the existing band-crossing dedup (`notifiedLimitKeys`); extracted the
  threshold-crossing decision out of `AppDelegate.checkAndNotify` into a pure,
  unit-tested `NotificationDecider`.
- Thresholds wired into the shared `QuotaBand.forLimit` evaluation (bands ranked
  by severity) — **not** a parallel percentage path. Users pick which band warns
  and which alerts via `NotificationThreshold` (tight / critical / exhausted).
- Added a global on/off toggle + two threshold pickers to Settings, persisted via
  `NotificationPreferencesStore` (same `UserDefaults` pattern as
  `DockVisibilityStore`).
- Decider gates: never notify when globally off, when the provider is disabled,
  or when the metric is stale (>1h old vs `lastUpdated`).

**2c — Launch at Login (#27):**
- `LaunchAtLoginControlling` protocol seam + `SMAppServiceLaunchAtLogin`
  (`SMAppService.mainApp`) concrete wrapper.
- `LaunchAtLoginStore` holds status, drives register/unregister, surfaces
  registration errors inline, and re-reads `SMAppService.mainApp.status` on
  Settings appear (it can change behind the app's back).
- Settings row (Toggle + inline error) added to the General section.

### Files changed

- `MeterBar/Services/NotificationDecider.swift` - new pure decision + gates
- `MeterBar/Models/NotificationThreshold.swift` - new threshold enum + prefs value
- `MeterBar/Services/NotificationPreferencesStore.swift` - new UserDefaults store
- `MeterBar/Services/LaunchAtLoginService.swift` - new protocol seam + SMAppService wrapper
- `MeterBar/Services/LaunchAtLoginStore.swift` - new toggle/status store
- `MeterBar/App/MeterBarApp.swift` - `checkAndNotify` now delegates to the decider
- `MeterBar/Views/SettingsView.swift` - Notifications section + Launch at Login row
- `MeterBar/Models/StorageKeys.swift` - 3 new keys
- `MeterBarTests/NotificationDeciderTests.swift` - crossing matrix + gates
- `MeterBarTests/NotificationPreferencesStoreTests.swift` - defaults/persistence
- `MeterBarTests/LaunchAtLoginStoreTests.swift` - toggle logic via fake controller

### Decisions

- **Decision:** Put all decision/toggle logic in SwiftPM-compiled files, keep the
  `MeterBarApp.swift` (AppDelegate) edit trivial.
  - **Context:** The SwiftPM `MeterBar` target excludes `App/MeterBarApp.swift`;
    PR CI only runs `swift build` (SwiftPM) + `swift test`, not the full
    `xcodebuild`. Logic left in the delegate would be neither compiled nor tested
    on PRs.
  - **Rationale:** The pure `NotificationDecider` / stores are fully compiled and
    unit-tested; the delegate just threads the dedup set and formats copy.

- **Decision:** Thresholds map to `QuotaBand`, ranked by severity, rather than a
  new numeric threshold.
  - **Context:** #23 explicitly requires wiring into the existing band evaluation.
  - **Rationale:** One source of truth for "how full is this quota"; defaults
    (warn `.critical`, alert `.exhausted`) reproduce the prior behavior exactly.

- **Decision:** 1-hour staleness bound.
  - **Rationale:** Longer than every non-manual refresh interval (max 30 min) so
    fresh data is never suppressed, short enough to drop an ancient cache.

### Mistakes and fixes

- **Mistake:** N/A — no rework this session.
- **Note:** `swift test` and SwiftLint can't run locally (CLT-only: no XCTest
  module, no sourcekitd). Verified via `swift build` (library compiles) + manual
  line-length/whitespace scan; the XCTest suite + SwiftLint run on CI.

### Next steps

- [ ] Confirm PR CI green (build + tests + coverage + lint).

---

**Total sessions today:** 1
