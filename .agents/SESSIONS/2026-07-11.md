# 2026-07-11

## SwiftPM/Xcode isolation parity: `.defaultIsolation(MainActor.self)` in Package.swift

### Problem
The Xcode app target builds with `SWIFT_DEFAULT_ACTOR_ISOLATION = MainActor`, but
`Package.swift` built the same MeterBar sources without it. `swift test` therefore
compiled the code with different actor-isolation semantics than the shipping app and
could not catch main-thread-blocking regressions (this hid the beachball-on-data-load
bug). Worse, the Xcode build was already emitting ~34 Swift-6-mode concurrency
warnings for the same mismatches; the SwiftPM build showed none.

### Change
- `Package.swift`: added `.defaultIsolation(MainActor.self)` to the `MeterBar` and
  `MeterBarTests` targets (matching the app's build settings).
- Fixed every isolation diagnostic that surfaced, by annotation only — no
  `Task.detached` / queue hops were removed:
  - **Type-level `nonisolated`** on pure value types and self-synchronized helpers
    that the SessionWake actors and detached tasks use: `ClaudeCodeAccount`,
    `ServiceSupport`, `StorageKeys`, `AppLog`, `CLIBinaryLocator`,
    `CodexHomeDirectory`, `AccountActivityInspector`, `ProviderReadinessInspector`,
    `ClaudeCodeCLIUsageService` (+ parser/error), `SharedDataStore` (now `final`,
    `@unchecked Sendable` — queue-serialized), `FlexibleISO8601`, `UsageFormat`,
    the `TokenCost.swift` / `CostSummaryStore.swift` / `ApiUsage.swift` /
    `OptimizationInsights.swift` model types, and the whole SessionWake value layer
    (`WakePaths`, `TranscriptClassifier`, `TranscriptResetParser`, `WakeBlockReason`,
    `WakeBounds`, `WakeQuota`, `WakeQuotaAuthority`, `WakeLock`, `ManagedProcess`,
    `WakeCommand*`, `WakeRunLogger`, `WakeProcessRunner`, `WakeSession` types,
    `WakeWatcherState` types, `WakeExecuting`/`WakeWatching`/`WakeQuotaProviding`
    protocols).
  - **Member-level `nonisolated`** on service methods that deliberately run off-main
    (`checkAccess`, keychain/auth-file/SQLite readers, `probeReadinessDatabase`,
    `CostTracker`'s pure static scan/parse helpers) plus the `shared` singletons and
    `private init`s of `ClaudeCode`/`Codex`/`Cursor` local services so they are
    referenceable from nonisolated default arguments and detached tasks.
  - `ServiceSupport.applyOnMain` now takes a `@MainActor @Sendable` closure
    (`MainActor.assumeIsolated` on the main-thread fast path), so `@Published`
    mutations inside it are properly isolation-checked.
  - `UsageDataManager` / `SessionWakeController` inits: MainActor store singletons
    can't appear in (nonisolated) default-argument position → `nil`-sentinel
    defaults resolved in the init body.
  - `CodexCliLocalService.authFileDataProvider` is now `@Sendable`.
  - Test fixes: `nonisolated` on a JSON fixture helper, `WatchRecorder`/`FakeWatcher`
    fakes, and a captured-`tempDir` hoist in `WakeRunnerTests`.

### Verification
- `swift package clean && swift test`: 445/445 pass, **zero warnings**.
- `xcodebuild -project MeterBar.xcodeproj -scheme MeterBar clean build`: succeeds;
  the ~34 pre-existing concurrency warnings are gone. Only remaining warning is the
  pre-existing (unrelated) `AccentColor` asset-catalog warning.
- `MeterBarCLI` (`swift build` in `MeterBarCLI/`): clean — the CLI consumes the
  library with the new isolation and the shared types are now explicitly
  `nonisolated` in every target that compiles them.
